install_subdir(
    'usr' / 'bin',
    install_dir: bindir,
    strip_directory: true,
    follow_symlinks: true,
)

install_subdir(
    'usr' / 'share',
    install_dir: prefix,
    follow_symlinks: true,
)
